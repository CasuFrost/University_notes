Un grafo G si dice s-t connesso se ogni vertice è contenuto in un cammino da s a t. 

Voglio lo pseudocodice che dato un grafo mi restituisce il più grande sottografo 
G' (massimale) che sia s-t connesso.


grafo_s-t_connesso(nodo s, nodo t){
	Set glob
	list visitedNode = {}
	DFSRec(G,s,glob,visitedNode,t)
	return glob
}

DFSRec(graph G, vert x, glob Set, visitedNodes lista, node t){
	visitedNodes.insert(x)
	for each y adiacente ad x {
		if(y==t){
			glob U visitedNodes
			return;
		}
		tmp = visitedNodes.copy()
		DFSRec(G,t,glob,tmp, t)
	}
}

//Soluzione del prof

Visited : array lungo n
massimale(s,t)
for (v in V){
	Visited[v]=-1
}
VisitaCammini(s,t)
if(Visited[s] != 1){
	for (v in V){
		Visited[v]=0
	}
}
return


VisitaCammini(u,t){
	if(Visited[u]!=-1) {return}
	if(u==t){
		Visited[u]=1
		return
	}
	k=0
	for(v adiacente di u){
		VisitaCammini(v,t)
		if(Visited[v]>k){ k = Visited[v] }
	}
	Visited[v]=k
	return
}






